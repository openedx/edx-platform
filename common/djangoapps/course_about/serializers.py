"""
These Serializers are used along side the default data module, in order to combine multiple data sources
into a single serializable dictionary.

These serializers should be accessed by the data module, but never directly by other modules in the API.

"""
