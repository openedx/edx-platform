name: Run make upgrade manually

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Target branch to create requirements PR against"
        required: true
        default: "master"
        type: string

defaults:
  run:
    shell: bash # making this explicit opts into -e -o pipefail

jobs:
  run-make-upgrade:
    runs-on: ubuntu-latest

    steps:
      - name: Check out target branch
        uses: actions/checkout@v4
        with:
          ref: "${{ inputs.branch }}"

      - name: Set up Python environment
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run make upgrade
        env:
          PACKAGE: "${{ inputs.package }}"
        run: |
          make upgrade

      - name: PR preflight
        run: |
          if git diff --exit-code; then
              # Fail early (and avoid quiet failure of create-pull-request action)
              echo "Error: No changes, so not creating PR." | tee -a "$GITHUB_STEP_SUMMARY"
              exit 1
          fi

      - name: Make a PR
        id: make-pr
        uses: peter-evans/create-pull-request@v6
        with:
          branch: "${{ github.triggering_actor }}/run-make-upgrade"
          branch-suffix: short-commit-hash
          add-paths: |
            requirements
            scripts/**/requirements*
          commit-message: |
            chore: Run make upgrade

            Commit generated by workflow `${{ github.workflow_ref }}`
          title: "chore: Run make upgrade"
          body: >-
            PR generated by workflow `${{ github.workflow_ref }}` to run `make upgrade` 
            on behalf of @${{ github.triggering_actor }}.
          assignees: "${{ github.triggering_actor }}"
          reviewers: "${{ github.triggering_actor }}"

      - name: Job summary
        env:
          PR_URL: "${{ steps.make-pr.outputs.pull-request-url }}"
        run: |
          if [[ -z "$PR_URL" ]]; then
              echo "PR not created; see log for more information" | tee -a "$GITHUB_STEP_SUMMARY"
              exit 1
          else
              echo "PR created or updated: $PR_URL" | tee -a "$GITHUB_STEP_SUMMARY"
          fi
