<%namespace file="../main.html" import="stanford_theme_enabled" />
Вы получили запрос о смене почтового адреса для учетной записи на курсах повышения квалификации учителей с ${old_email} на ${new_email}.
Если это верно, то подтвердите свой запрос, пройдя по указанной ниже ссылке:

% if is_secure:
 https://${ site }/email_confirm/${ key }
% else:
 http://${ site }/email_confirm/${ key }
% endif

Техническая поддержка курсов повышения квалификации ГБОУ ЦПМ
