
    # Set the display to the virtual frame buffer (Xvfb)
    export DISPLAY=:99

    # Set the display to the virtual frame buffer (Xvfb)
    export DISPLAY=:99

    # Set the display to the virtual frame buffer (Xvfb)
    export DISPLAY=:99
